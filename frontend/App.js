import React from 'react';

function App() {
    return ( <
        div className = "App"
        style = {
            { textAlign: 'center', marginTop: '50px', fontFamily: 'Arial' } } >
        <
        h1 > DevOps Readiness App < /h1> <
        p > This app is ready
        for Docker, Kubernetes, and CI / CD pipelines. < /p> <
        /div>
    );
}

export default App;